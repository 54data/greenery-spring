<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mycompany.miniproject.dao.ProductDao">

	<select id="getProductAll" parameterType="Pager" resultType="Product">
	<![CDATA[
		select product_id, product_name, product_price, product_summary, category
		from(
			select rownum rnum, product_id, product_name, product_price, product_summary, category
			from (
				select product_id, product_name, product_price, product_summary, category
				from product
				order by product_id desc
			)
			where rownum <= #{endRowNo}
		)
		where rnum >= #{startRowNo}
	]]>
	</select>
	
	<select id="countRows" resultType="int">
		select count(*)
		from product
	</select>
	
	<select id="getCategoryProduct" parameterType="String" resultType="Product">
		select
			product_id, product_name, product_price, product_summary, category
		from product
		where category=#{category}
	</select>
	
	
	<select id = "getProductDetailInfo" parameterType="int" resultType="Product">
		select * 
		from product
		where product_Id = #{productId}
	</select>
</mapper>